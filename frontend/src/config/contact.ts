/**
 * Dane kontaktowe autora aplikacji — jedno źródło prawdy.
 *
 * Te same dane pojawiają się w dokumentacji (`docs/reference/kontakt.md`, stopka
 * MkDocs), w `README.md` oraz w metadanych instalatora (`Cargo.toml`). Przy zmianie
 * kontaktu trzeba poprawić także tamte miejsca — nie da się ich zasilić stąd,
 * bo powstają poza buildem frontendu.
 */
export const APP_AUTHOR = "Jakub Ślesiński";
export const APP_CONTACT_EMAIL = "jakub.slesinski@wat.edu.pl";
